# geo-crm-api

API FastAPI du GEO-CRM. Installation depuis la racine du dépôt :

```bash
python -m pip install -e . -e geo_crm/backend
```

Voir `geo_crm/README.md` pour le lancement et la configuration.

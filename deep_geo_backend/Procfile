# Processus lancés ensemble par : honcho start (depuis ce dossier, venv activé)
web: python -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8010
worker: python -m celery -A app.worker.celery_app worker --loglevel=info --pool=solo --concurrency=1
